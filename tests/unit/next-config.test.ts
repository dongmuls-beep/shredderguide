// @vitest-environment node  import { describe, expect, it } from "vitest";  describe("next config", () => {   it("sets basePath for production github pages", async () => {     process.env.NODE_ENV = "production";     process.env.GITHUB_REPOSITORY = "copyland/shredderguide";      const configModule = await import("../../next.config.ts?test=basepath");     const config = configModule.default;      expect(config.basePath).toBe("/shredderguide");     expect(config.assetPrefix).toBe("/shredderguide");   }); });