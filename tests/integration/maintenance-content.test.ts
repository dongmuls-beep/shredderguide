// @vitest-environment node  import { describe, expect, it } from "vitest";  import { getMaintenanceContent } from "@/lib/maintenance-content";  describe("maintenance content loader", () => {   it("loads korean markdown", async () => {     const markdown = await getMaintenanceContent("ko");     expect(markdown).toContain("오일링");   });    it("loads english markdown", async () => {     const markdown = await getMaintenanceContent("en");     expect(markdown).toContain("Oiling");   }); });