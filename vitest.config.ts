import path from "node:path"; import { fileURLToPath } from "node:url";  import react from "@vitejs/plugin-react"; import { defineConfig } from "vitest/config"; import tsconfigPaths from "vite-tsconfig-paths";  const root = path.dirname(fileURLToPath(import.meta.url));  export default defineConfig({   plugins: [react(), tsconfigPaths()],   test: {     environment: "jsdom",     setupFiles: [path.resolve(root, "tests/setup.ts")],     include: ["tests/**/*.test.ts", "tests/**/*.test.tsx"],     globals: true,     coverage: {       reporter: ["text", "lcov"],       include: ["lib/**/*.{ts,tsx}", "components/**/*.{ts,tsx}", "scripts/**/*.mjs"]     }   } });