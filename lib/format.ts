import type { Locale } from "@/lib/types";  export function formatCurrency(value: number, locale: Locale): string {   const formatter = new Intl.NumberFormat(locale === "ko" ? "ko-KR" : "en-US", {     style: "currency",     currency: "KRW",     maximumFractionDigits: 0   });    return formatter.format(value); }  export function formatNumber(value: number, locale: Locale): string {   return new Intl.NumberFormat(locale === "ko" ? "ko-KR" : "en-US").format(value); }