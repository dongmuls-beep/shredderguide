import { readFile } from "node:fs/promises"; import path from "node:path";  import type { Locale } from "@/lib/types";  export async function getMaintenanceContent(locale: Locale): Promise<string> {   const filename = locale === "ko" ? "maintenance.ko.md" : "maintenance.en.md";   const filePath = path.join(process.cwd(), "content", filename);   return readFile(filePath, "utf-8"); }